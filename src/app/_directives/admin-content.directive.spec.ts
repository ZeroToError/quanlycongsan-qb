import { AdminContentDirective } from './admin-content.directive';

describe('AdminContentDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminContentDirective();
    expect(directive).toBeTruthy();
  });
});
